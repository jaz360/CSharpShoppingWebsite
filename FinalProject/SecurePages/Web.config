<?xml version="1.0"?>
<configuration>
    <system.web>

      <authorization>
        <deny users="?"/>
        <!-- ? means anonymous users -->
      </authorization>

    </system.web>
</configuration>
